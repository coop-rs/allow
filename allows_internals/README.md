Internal - to be used by `allows` crate only.

This could be simplified, or even eliminated (replaced + integrated into `allows` itself) if we used https://crates.io/crates/paste. Up to the future maintainers.
