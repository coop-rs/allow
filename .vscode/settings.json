{
    "colorTabs.config": [
        {
            "regex": "allows/.*"
        },
        {
            "regex": "allows_internals/.*"
        },
        {
            "regex": "allows_tests/.*"
        }
    ]
}